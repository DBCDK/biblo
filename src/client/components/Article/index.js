/**
 * @file
 * entry point for the search page
 */

import {renderComponent} from '../../App';
import Article from './Article.component.js';

/**
 * Client side rendering of the Article Component
 */
renderComponent(Article, 'content');
