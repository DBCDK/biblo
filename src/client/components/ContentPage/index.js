/**
 * @file
 * entry point for the search page
 */

import {renderComponent} from '../../App';
import ContentPage from './ContentPage.component';

/**
 * Client side rendering of the ContentPage Component
 */
renderComponent(ContentPage, 'content');
