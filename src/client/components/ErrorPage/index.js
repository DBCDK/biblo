/**
 * @file
 * entry point for the error page
 */

import ErrorPageContainer from './ErrorPageContainer.component';
import {renderComponent} from '../../App';

/**
 * Client side rendering of the ErrorPage Component
 */
renderComponent(ErrorPageContainer, 'content');
