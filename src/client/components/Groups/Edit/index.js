/**
 * @file
 * entry point for edit group page
 */
import {renderComponent} from '../../../App';
import GroupEditContainer from './GroupEditContainer.component';

/**
 * Client side rendering of the Frontpage Component
 */
renderComponent(GroupEditContainer, 'content');
