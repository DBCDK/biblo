'use strict';

import React from 'react';
import TestUtils from 'react-addons-test-utils';
import {expect} from 'chai';
//import sd from 'skin-deep';

import PostView from '../GroupList.component';

/*
describe('Test of GroupList', () => {
    const props = {
        id: 1,
        groupId: 1,
        content: 'test content',
        image: 'some_url',
        timeCreated: (new Date()).toISOString(),
        owner: {
            displayName: 'testUser',
            image: 'some_profile_image'
        },
        profile: {
            userIsLoggedIn: true
        }
    };

    function mockGroup () {
    }

    it('should generate html', () => {
        //const tree = sd.shallowRender(<PostView {...props} />);
        //expect(tree.subTree('.post-profile-image').subTree('img').getRenderOutput().props.src).to.be.equal(props.owner.image);
        //expect(tree.subTree('.post-profile-image').subTree('img').getRenderOutput().props.alt).to.be.equal(props.owner.displayName);
        //expect(tree.subTree('.username').text()).to.be.equal(props.owner.displayName);
        //expect(tree.subTree('.time').text()).to.be.equal('Lige nu');
        //expect(tree.subTree('.content').text()).to.be.equal(props.content);
        //expect(tree.subTree('.media').subTree('img').getRenderOutput().props.src).to.be.equal(props.image);
    });

    it('should show an image on first group', () => {
        props.image = null;
        const tree = sd.shallowRender(<PostView {...props} />);
        expect(tree.subTree('.media')).to.be.equal(false);
    });

    it('should show the group title', () => {

    });

    it('should report how many followers a group has', () => {

    });

    it('should show the 15 latest groups', () => {

    });

});
*/